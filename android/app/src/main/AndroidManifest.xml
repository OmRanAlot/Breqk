<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" 
        tools:ignore="ProtectedPermissions"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" 
        tools:ignore="QueryAllPackagesPermission" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    
    <!-- VPN permission (if you want to keep VPN functionality) -->
    <uses-permission android:name="android.permission.BIND_VPN_SERVICE"
        tools:ignore="ProtectedPermissions" />

    <!-- Exact alarms for mode scheduling (Bedtime, Study Mode, etc.) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:supportsRtl="true">

        <!-- VPN Service (optional - mainly using UsageStatsManager instead) -->
        <service
            android:name=".MyVpnService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

        <!-- Browser URL-based content filtering -->
        <service
            android:name=".ContentFilterService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/content_filter_accessibility_config" />
        </service>

        <!-- Instagram Reels and YouTube Shorts scroll intervention -->
        <service
            android:name=".ReelsInterventionService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/reels_intervention_service_config" />
        </service>

        <!--
            Device Administrator receiver.
            Registers Breqk as a Device Admin so it cannot be uninstalled without
            first deactivating admin privileges — deliberate friction against deletion.
            Logcat tag: DEVICE_ADMIN

            DEV bypass (ADB):
              adb shell dpm remove-active-admin com.breqk/.BreqkDeviceAdminReceiver
              adb uninstall com.breqk
        -->
        <receiver
            android:name=".BreqkDeviceAdminReceiver"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.DEVICE_ADMIN_DISABLE_REQUESTED" />
                <action android:name="android.app.action.DEVICE_ADMIN_DISABLED" />
            </intent-filter>
        </receiver>

        <!--
            Mode schedule receiver.
            Handles AlarmManager intents for mode auto-activation/deactivation
            (e.g., Bedtime mode at 10pm) and re-registers alarms on boot.
            Logcat tag: MODE_SCHED
        -->
        <receiver
            android:name=".ModeSchedulerReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="com.breqk.ACTION_MODE_START" />
                <action android:name="com.breqk.ACTION_MODE_END" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".BreqkWidgetProvider"
            android:exported="true"
            android:label="@string/widget_description">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_breqk_info" />
        </receiver>

        <!--
            Entry point — shown on every cold launch until ReelsInterventionService is enabled.
            Forwards to MainActivity automatically once permission is granted.
            Logcat tag: ACC_PERM_GATE
        -->
        <activity
            android:name=".AccessibilityPermissionActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Main React Native app screen.
            Only reached after AccessibilityPermissionActivity confirms the service is enabled.
            Launcher intent filter removed — now lives on AccessibilityPermissionActivity.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:windowSoftInputMode="adjustResize">

            <!-- Deep link for widget browser quick-launch buttons (breqk://browser/instagram, breqk://browser/youtube) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="breqk" android:host="browser" />
            </intent-filter>
        </activity>
    </application>
</manifest>